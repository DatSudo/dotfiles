function acs(){
  (( $+commands[python] )) || {
    echo "[error] No python executable detected"
    return
  }
  alias | python $HOME/.local/bin/alias_cheatsheet.py $@
}

function crun(){
    gcc $1 && ./a.out
}
